# 🚀 NeuroCMS - چک‌لیست پیشرفت پروژه

## فاز ۱: بنیان امن و پایدار (اولویت: بحرانی)
- [ ] **۱.۱. حذف فایل `.env` از مخزن**
  - [ ] حذف فایل `.env` از دیسک.
  - [ ] پاک کردن تاریخچه فایل `.env` از گیت (با `git filter-repo` یا `BFG`).
  - [ ] اطمینان از اینکه `.gitignore` شامل `.env` است.
- [ ] **۱.۲. پاکسازی و یکپارچه‌سازی ساختار**
  - [ ] مقایسه پوشه `neurocms/` با ساختار اصلی (`app/`, `public/`).
  - [ ] انتقال فایل‌های معتبر به جای درست.
  - [ ] حذف پوشه تکراری `neurocms/`.
- [ ] **۱.۳. راه‌اندازی محیط توسعه**
  - [ ] ایجاد یک فایل `.env.example` کامل و به‌روز.
  - [ ] نوشتن دستورات نصب در `README.md`.
  - [ ] تست فرآیند نصب از صفر (`/install`).

## فاز ۲: هسته هوشمند (AI Kernel) (اولویت: بالا)
- [ ] **۲.۱. پیاده‌سازی سرویس‌های AI**
  - [ ] تکمیل متد `analyze()` در `AiService` (ارسال درخواست به OpenAI/Claude).
  - [ ] تکمیل متد `generate()` برای تولید محتوا.
  - [ ] تکمیل متد `monitorSystem()` برای نظارت بر سلامت سیستم.
- [ ] **۲.۲. سیستم یادگیری (LearningService)**
  - [ ] طراحی ساختار داده برای ذخیره تعاملات.
  - [ ] پیاده‌سازی منطق ساده‌ی یادگیری از رفتار کاربر.
- [ ] **۲.۳. دستیار هوشمند (Smart Assistant)**
  - [ ] ایجاد یک واسط خط فرمان یا ویجت در پنل مدیریت.
  - [ ] امکان دریافت فرامین طبیعی (مثلاً "یک گزارش فروش بساز").
  - [ ] اتصال دستیار به سایر ماژول‌ها (مثلاً `ReportGenerator`).

## فاز ۳: تکمیل ماژول‌های اصلی (اولویت: متوسط)
- [ ] **۳.۱. سازنده‌ها (Makers)**
  - [ ] تکمیل `ComponentMaker` (ایجاد کامپوننت‌های قابل استفاده مجدد).
  - [ ] تکمیل `PluginMaker` (ایجاد پلاگین‌های مستقل).
  - [ ] بهبود `ModuleMaker` برای تولید ماژول‌های کامل‌تر.
- [ ] **۳.۲. مدیریت محتوا**
  - [ ] تکمیل `FormCreator` برای ساخت فرم‌های پویا.
  - [ ] تکمیل `TableGenerator` برای نمایش داده‌ها.
  - [ ] تکمیل `ReportGenerator` برای تولید گزارش‌های پیشرفته.
- [ ] **۳.۳. مدیریت قالب و زبان**
  - [ ] تکمیل `TemplateManager` با قابلیت ساخت قالب توسط AI.
  - [ ] تکمیل `LanguageManager` برای پشتیبانی کامل از چندزبانه بودن.

## فاز ۴: تجربه کاربری و قالب‌ها (اولویت: تکمیلی)
- [ ] **۴.۱. پنل مدیریت**
  - [ ] طراحی داشبورد ادمین با استفاده از `TemplateManager`.
  - [ ] بهینه‌سازی منوها و مسیرهای دسترسی.
- [ ] **۴.۲. قالب‌های عمومی (فرانت‌اند)**
  - [ ] طراحی یک قالب پیش‌فرض زیبا و واکنش‌گرا.
  - [ ] امکان تغییر قالب از طریق پنل مدیریت.
- [ ] **۴.۳. فرآیند نصب**
  - [ ] بهبود ویزارد نصب (`/install`) با بررسی کامل نیازمندی‌ها.
  - [ ] ایجاد قابلیت نصب نمونه محتوای اولیه.

## فاز ۵: بهینه‌سازی و انتشار (اولویت: تکمیلی)
- [ ] **۵.۱. امنیت و عملکرد**
  - [ ] اجرای تست نفوذپذیری اولیه.
  - [ ] بهینه‌سازی کوئری‌ها و افزایش سرعت بارگذاری.
- [ ] **۵.۲. مستندسازی**
  - [ ] نوشتن مستندات کامل برای توسعه‌دهندگان (API، ساختار ماژول‌ها).
  - [ ] نوشتن راهنمای کاربری.
- [ ] **۵.۳. انتشار**
  - [ ] برچسب‌گذاری نسخه‌ها (Tagging) در گیت.
  - [ ] ایجاد بسته‌های نصب (ZIP) برای انتشار.
